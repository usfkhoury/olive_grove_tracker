# Database (live data — back it up separately)
data/

# Frontend
frontend/node_modules/
frontend/dist/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Claude Code local config
.claude/

# Local env overrides (commit .env.example, never .env)
.env
