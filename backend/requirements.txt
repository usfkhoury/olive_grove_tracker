# Pinned so a rebuild on the VM can't silently pull a breaking version.
# To upgrade: bump deliberately, run the tests (see requirements-dev.txt),
# then deploy.
fastapi==0.136.3
uvicorn[standard]==0.49.0
SQLAlchemy==2.0.50
pydantic==2.13.4
itsdangerous==2.2.0
google-auth==2.38.0
# Transport used by google-auth to fetch Google's signing certs (not a default dep).
requests==2.32.3
